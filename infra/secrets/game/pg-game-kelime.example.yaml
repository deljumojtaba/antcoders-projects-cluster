# Owner of kelime_prod. Use the SAME password as in kelime-config-seed.
apiVersion: v1
kind: Secret
metadata:
  name: pg-game-kelime
  namespace: game
type: kubernetes.io/basic-auth
stringData:
  username: kelime
  password: "..."        # openssl rand -hex 24
