# Google Play service account for Android IAP verification
# (kelime-savasi/backend/google-service-account.json).
apiVersion: v1
kind: Secret
metadata:
  name: kelime-google-sa
  namespace: game
stringData:
  google-service-account.json: |
    { "type": "service_account", "...": "..." }
