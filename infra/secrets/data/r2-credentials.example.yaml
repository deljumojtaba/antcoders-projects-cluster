# Cloudflare -> R2 -> Manage API tokens -> Create token with "Object Read &
# Write" on bucket antcoders-backups only.
apiVersion: v1
kind: Secret
metadata:
  name: r2-credentials
  namespace: data
stringData:
  ACCESS_KEY_ID: "..."
  ACCESS_SECRET_KEY: "..."
  REGION: auto
